  Assuming one wants to create a new Tag called "mytag" in "production"
  namespace, tracking images from "quay.io/repo/name:latest" and mirroring
  it into the cluster:

  $ kubectl tag new --from quay.io/repo/name:latest --mirror mytag -n production

